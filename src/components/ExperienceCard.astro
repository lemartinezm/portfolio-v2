---
import Badge from "./Badge.astro";

interface Props {
  time: string;
  position: string;
  company: string;
  companyPage: string;
  description: string;
  skills: string[];
}

const { time, position, company, companyPage, description, skills } =
  Astro.props;
---

<li class="card">
  <div class="timeline">
    <div class="circle"></div>
    <div class="line"></div>
  </div>
  <div class="content">
    <span class="time">{time}</span>
    <h3>
      {position} - <a
        href={companyPage}
        target="_blank"
        rel="noopener noreferrer"
      >
        {company}
      </a>
    </h3>
    <p>{description}</p>
    <div class="skills">
      {skills.map((skill) => <Badge name={skill} />)}
    </div>
  </div>
</li>

<style>
  .card {
    display: flex;
    list-style-type: none;
    padding: 0;
  }

  .card:hover .timeline,
  .card:hover .time {
    color: #3498db;
  }

  .timeline {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    transition: color 0.2s;
  }

  .time {
    transition: color 0.2s;
  }

  .circle {
    width: 1rem;
    height: 1rem;
    background-color: currentColor;
    border-radius: 50%;
  }

  .line {
    border-left: 1px solid #fff;
    flex: 1;
  }

  .content {
    padding: 0 1rem 2rem 1rem;
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }
  h3 {
    color: #fff;
    font-size: 1.5rem;
    margin: 0;
  }

  p {
    font-size: 1.125rem;
    margin: 0;
  }
  a,
  a:visited {
    color: #fff;
    text-decoration: none;
  }
  .skills {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }
</style>
